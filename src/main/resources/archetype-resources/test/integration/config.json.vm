{
    "parameters": {
        "domain": "${proxydomain}",
        "protocol": "${proxyscheme}",
        "port":  "${proxyport}",
        "basepath": "${basepath}${deployment.suffix}",
        "apps": {
            "${artifactId}-app-cicd${deployment.suffix}": {
                "name": "${artifactId}-app-cicd${deployment.suffix}",
                "appHeaderName": "Transaction-Id",
                "appHeaderValue": "123456789"
            }
        }
    }
}